<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/ripple_spinner_view">

    <TextView
        android:id="@+id/spinnerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/stub"
        android:gravity="center_vertical"
        android:textAppearance="@style/SecondaryTextAppearance"
        android:textColor="@color/EditTextTextColorHints"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/spinnerText"
        app:layout_constraintVertical_chainStyle="packed"/>

    <TextView
        android:id="@+id/spinnerText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/stub"
        android:gravity="center_vertical"
        android:textAppearance="@style/SecondaryTextAppearance"
        android:textColor="@color/TextColorSecondary"
        app:layout_constraintTop_toBottomOf="@id/spinnerTitle"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>