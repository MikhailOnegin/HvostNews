<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/rvInterestHeight"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/background_rv_interest">

    <ru.hvost.news.presentation.customviews.RoundCornersImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:cornersRadius="@dimen/rvImageCorners"
        android:scaleType="centerCrop"
        android:layout_margin="@dimen/rvImageMargins"/>

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/rvInterestPadding"
        android:layout_marginBottom="@dimen/rvInterestPadding"
        android:text="@string/stub"
        android:textAppearance="@style/RegTextTextAppearance"
        android:textColor="@color/sexSelector"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textSize="@dimen/rvInterestTextSize"/>

    <View
        android:id="@+id/selection"
        android:layout_width="@dimen/rvInterestCircleSize"
        android:layout_height="@dimen/rvInterestCircleSize"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="@dimen/rvInterestPadding"
        android:layout_marginStart="@dimen/rvInterestPadding"
        android:background="@drawable/background_rv_selection"/>

</androidx.constraintlayout.widget.ConstraintLayout>